model:
  model_root: "/external1/pretrained_models/speech-tagger_hi_ctc/"
  model_name: "Conformer-CTC-BPE-Large.nemo"
  tokenizer_folder: "tokenizer"
  new_tokenizer_folder: "model_manifest_tokenizer"
  proto_file: "proto/file"
  proto_dir: "proto/dir"

training:
  data_dir: "/external4/datasets/AI4Bharat/hindi"
  train_manifest: "nemo_train.jsonl"
  test_manifest: "nemo_valid.jsonl"
  batch_size: 4  # Small batch size for testing
  max_steps: 100000  # Small max_steps for initial testing

experiment:
  exp_dir: "/external1/experiments/"
  exp_name: "hi-model-manifest-v3-tokenizer-extended"
  monitor: "val_wer"
  mode: "min"
  always_save_nemo: true
  save_best_model: true
